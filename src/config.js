export const protHttp = 'https://';
export const domain = 'norma.nomoreparties.space';
export const apiUrl = domain + '/api';

export const getIngredientsUrl = protHttp + apiUrl + '/ingredients';
export const createOrderUrl = protHttp + apiUrl + '/orders';

export const ForgotUrl = protHttp + apiUrl + '/password-reset';
export const ResetUrl = protHttp + apiUrl + '/password-reset/reset';

export const registerUrl = protHttp + apiUrl + '/auth/register';

export const loginUrl = protHttp + apiUrl + '/auth/login';
export const logoutUrl = protHttp + apiUrl + '/auth/logout';
export const tokenUrl = protHttp + apiUrl + '/auth/token';
export const userUrl = protHttp + apiUrl + '/auth/user';

